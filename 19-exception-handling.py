import sys

def spam(devideBy):
	try :
		return (devideBy/0)
		return (devideBy/0)
		return (devideBy/0)
	except ZeroDivisionError:
		print("Error: zero dvision error")
spam(33)

import requests

requests.get("https://www.google.com")

requests.get()

input("add something")
requests.get()
